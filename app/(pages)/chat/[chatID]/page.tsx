import Chatcomponent from "@/app/components/Chatcomponent";

/**
 * Home component rendering the Chatcomponent dynamically.
 */
const Home = () => {
  // Render the Chatcomponent
  return <Chatcomponent />;
};

export default Home;
